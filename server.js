// This file is created to work with the package.json script
// The actual server code is in server.ts, but we need this for the npm run api script
console.log("Please compile server.ts to JavaScript first or run with ts-node.");
console.log("For development, use: npx ts-node server.ts");
console.log("For production, compile with: npx tsc && node dist/server.js");